# SDK Coordinates

- Domain: app
- Language: python
- SDK package directory: `sdkwork-app-sdk-python`
- Build descriptor: `pyproject.toml`
- SDK coordinate: `sdkwork-app-sdk==1.0.0`

## Integration Commands

- Registry mode: `pip install sdkwork-app-sdk==1.0.0`
- Local mode: `pip install <path-to-sdkwork-app-sdk-python>`

## Validation Commands

- Stage 1: `python -m pip show sdkwork-app-sdk`
- Stage 5: `python -m pytest`

